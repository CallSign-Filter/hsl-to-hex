{
  "name": "@usmchessler/hsl-to-hex",
  "version": "1.0.2",
  "description": "Convert HSL colors to RGB colors in hex format",
  "main": "index.js",
  "scripts": {
    "check": "npm ls && npm test",
    "test": "tap --cov test",
    "prepublish": "npm run check && ./publish-dep.sh"
  },
  "author": "HesslerB",
  "license": "ISC",
  "dependencies": {
    "debug": "^4.2.0",
    "hsl-to-rgb-for-reals": "^1.1.1",
    "lint": "^0.7.0"
  },
  "devDependencies": {
    "tap": "^14.10.8"
  },
  "directories": {
    "test": "test"
  }
}